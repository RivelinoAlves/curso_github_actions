name: GitHub Actions - Worflow Dispatch Template
run-name: Running - ${{ github.event_name }}
on: [workflow_dispatch]

jobs:
  on-workflow_dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Comentário
        run: |
         echo "Comentário da Execução do Worflow Dispatch"